import { useDispatch } from "react-redux";
import { useNavigate } from "react-router-dom";

export function DisplayLectures() {

    const dispatch = useDispatch();
    const navigate = useNavigate();


}